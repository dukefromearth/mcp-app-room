name: conformance-tier2

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  conformance-tier2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Tier 2 conformance gate
        run: npm run conformance:tier2

      - name: Upload conformance artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conformance-tier2-artifacts
          path: artifacts/conformance
